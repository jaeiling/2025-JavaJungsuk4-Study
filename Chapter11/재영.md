# Chapter 11 — 컬렉션 프레임워크 (1.1~1.7)

---

## 1. 컬렉션 프레임워크란?

* **데이터 군(群)을 저장하는 클래스들을 표준화한 설계**
* **Collections** = 다수의 데이터, **Framework** = 표준화된 구조
* → 다수의 데이터를 효율적으로 저장·관리·처리하기 위한 **객체 지향적 구조**

> 자바 API에서는 “데이터 군을 다루고 표현하기 위한 단일화된 구조”로 정의.

### 주요 특징

* 인터페이스 + 다형성 기반 → 일관된 사용법 / 재사용성 높음.
* **핵심 인터페이스 3가지:** `List`, `Set`, `Map`

    * `List` / `Set`은 `Collection` 인터페이스의 하위 인터페이스
    * `Map`은 별도의 구조 (key-value 쌍)

| 인터페이스    | 특징                    | 구현 클래스 예시                               |
| -------- | --------------------- | --------------------------------------- |
| **List** | 순서 O, 중복 O            | ArrayList, LinkedList, Vector, Stack    |
| **Set**  | 순서 X, 중복 X            | HashSet, TreeSet                        |
| **Map**  | 키-값 쌍, 키 중복 X, 값 중복 O | HashMap, TreeMap, Hashtable, Properties |

> 🔸 **키(key)**는 값(value)을 찾기 위한 고유한 식별자이므로 중복 불가.

---

## 2. Collection 인터페이스

* **List / Set**이 공통으로 상속받는 인터페이스
* 데이터 추가, 삭제, 탐색 등 기본 기능 정의

```java
boolean add(E e)
boolean remove(Object o)
boolean contains(Object o)
int size()
void clear()
boolean isEmpty()
Iterator<E> iterator()
```

> `E`는 제네릭 타입 파라미터 (Element). `T`, `K`, `V` 등으로도 표기됨.

* `parallelStream()`, `forEach()` 등은 스트림(14장)에서 다룸.

---

## 3. List 인터페이스

* **순서가 유지되고 중복 허용**되는 자료구조
* 인덱스로 요소 접근 가능

```java
add(int index, E element)
get(int index)
set(int index, E element)
remove(int index)
indexOf(Object o)
lastIndexOf(Object o)
```

* 구현체 : `ArrayList`, `LinkedList`, `Vector`, `Stack`

---

## 4. Set 인터페이스

* **순서 유지 X, 중복 허용 X**
* 중복 제거가 필요한 데이터에 적합 (집합 연산)
* 구현체 : `HashSet`, `TreeSet`

```java
Set<String> set = new HashSet<>();
set.add("A");
set.add("B");
set.add("A"); // 중복 무시
```

---

## 5. Map 인터페이스

* **Key-Value 쌍으로 구성된 데이터 집합**
* **Key 중복 불가**, Value 중복 가능

```java
Map<String, Integer> map = new HashMap<>();
map.put("Apple", 3);
map.put("Banana", 5);
map.put("Apple", 7); // 기존 값 덮어씀
```

* `keySet()` → 모든 키 반환 (Set 타입)
* `values()` → 모든 값 반환 (Collection 타입)
* `entrySet()` → 키-값 쌍(Entry) 반환 (Set<Map.Entry>)

### 내부 인터페이스 — Map.Entry

```java
public interface Map<K,V> {
    interface Entry<K,V> {
        K getKey();
        V getValue();
        V setValue(V value);
    }
}
```

---

## 6. ArrayList

* **List 인터페이스 구현체**, 가장 많이 사용되는 컬렉션 클래스
* 내부적으로 **배열 기반** → 인덱스 접근 빠름 (검색 O, 추가/삭제 느림)
* 용량(capacity) 초과 시 자동으로 더 큰 배열 생성 후 복사

```java
ArrayList<String> list = new ArrayList<>();
list.add("A");
list.add("B");
list.remove("A");
list.get(0);
```

> **Vector**는 동기화 지원 (멀티스레드용), 하지만 구버전 클래스이므로 `ArrayList` 사용 권장.

### ArrayList 정렬

```java
Collections.sort(list); // 오름차순 정렬
Collections.sort(list, Collections.reverseOrder()); // 내림차순
```

> `Collection`은 인터페이스, `Collections`는 유틸리티 클래스임을 구분하자.

---

## 7. LinkedList

* **Node(데이터 + 다음/이전 노드 참조)**로 구성된 연결형 구조
* **비연속적 메모리 구조** → 삽입/삭제 빠름, 접근 느림

### 단일 연결 리스트 vs. 이중 연결 리스트

```java
class Node {
    Node next;     // 다음 노드
    Node previous; // 이전 노드 (이중 연결 리스트)
    Object data;
}
```

* 실제 `LinkedList`는 **이중 연결 리스트(Doubly Linked List)** 형태로 구현되어 있음.

| 구조             | 접근 속도 | 추가/삭제 | 비고          |
| -------------- | ----- | ----- | ----------- |
| **ArrayList**  | 빠름    | 느림    | 순차적 접근에 유리  |
| **LinkedList** | 느림    | 빠름    | 중간 삽입/삭제 유리 |

> 데이터 개수가 많지 않다면 차이 미미. 변경이 잦은 구조엔 LinkedList 권장.

---

## 8. Stack과 Queue

### Stack (스택)

* **LIFO (Last In, First Out)** 구조
* Vector 기반

```java
Stack<Integer> stack = new Stack<>();
stack.push(1);
stack.push(2);
System.out.println(stack.pop()); // 2
```

### Queue (큐)

* **FIFO (First In, First Out)** 구조
* `LinkedList`로 구현

```java
Queue<String> queue = new LinkedList<>();
queue.offer("A");
queue.offer("B");
System.out.println(queue.poll()); // A
```

> Stack은 Vector 기반, Queue는 LinkedList 기반으로 구현하는 것이 일반적.

---

## 💡 Iterator / Iterable 요약

* **Iterator**: 컬렉션의 요소를 순차적으로 접근하기 위한 인터페이스

```java
public interface Iterator<E> {
    boolean hasNext();
    E next();
    void remove();
}
```

* **Iterable**: “반복 가능한 객체”를 의미. `iterator()` 메서드를 반드시 구현해야 함.

```java
public interface Iterable<T> {
    Iterator<T> iterator();
}
```

* **Collection 인터페이스는 Iterable을 상속받음** → 모든 컬렉션은 `for-each`문 사용 가능.

```java
for (String s : list) {
    System.out.println(s);
}
```

---

## 💡 Comparator vs Comparable

| 구분             | 위치     | 목적       | 사용 예                                             |
| -------------- | ------ | -------- | ------------------------------------------------ |
| **Comparable** | 클래스 내부 | 기본 정렬 기준 | `Collections.sort(list)`                         |
| **Comparator** | 클래스 외부 | 별도 정렬 기준 | `Collections.sort(list, new CustomComparator())` |

```java
class Student implements Comparable<Student> {
    int score;
    public int compareTo(Student other) {
        return this.score - other.score; // 오름차순
    }
}

Collections.sort(list); // Comparable 기준 정렬

Collections.sort(list, (a,b) -> b.score - a.score); // Comparator: 내림차순 정렬
```

> **Comparable** = 클래스가 스스로 정렬 기준을 가짐.
> **Comparator** = 외부에서 정렬 기준을 주입.
